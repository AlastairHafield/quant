{
  "name": "quant",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-strategy quant backtesting app (PEAD, S&D Zones, Mean-Reversion Lab)",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node backend/src/index.js",
    "test": "node --test shared/test",
    "heroku-postbuild": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix gex-breakout && npm install --prefix mechanical-orb && npm install --prefix gap-continuation"
  },
  "dependencies": {
    "mongodb": "^7.5.0"
  }
}

{
  "name": "shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Risk-enforcement code used by gex-breakout — the one deliberate exception to this repo's otherwise self-contained-module-per-bot convention.",
  "scripts": {
    "test": "node --test"
  }
}

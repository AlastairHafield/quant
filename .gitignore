# dependencies
node_modules/
**/node_modules/

# secrets — never commit
.env
**/.env
!.env.example

# logs
*.log
npm-debug.log*

# local SQLite database (filesystem is ephemeral on Heroku anyway)
backend/data/*.db
backend/data/*.db-shm
backend/data/*.db-wal

# frontend build is produced on Heroku via heroku-postbuild
frontend/build/

# stray/duplicate scratch directories
{backend/
pead-trader/

# os
.DS_Store
Thumbs.db

# stray server logs
*.err

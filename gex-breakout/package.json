{
  "name": "gex-breakout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MES order-flow system: ADX-regime footprint/volume-profile confirmed breakout and mean-reversion signals (the Order Flow Bot), TopstepX-only",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node --env-file=.env src/worker.js",
    "test": "node --test"
  },
  "dependencies": {
    "@microsoft/signalr": "^10.0.0",
    "mongodb": "^7.5.0"
  }
}

{
  "name": "mechanical-orb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Validated 15-min ORB strategy (orb-alpaca-1m-findings): MES, LONG only, 1-min CLOSE trigger, ADX-filtered — live at flat 1 contract for monitoring",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node --env-file=.env src/worker.js",
    "test": "node --test"
  },
  "dependencies": {
    "@microsoft/signalr": "^10.0.0"
  }
}
